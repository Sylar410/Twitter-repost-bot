# .env.example - copy to .env for local testing (do NOT commit .env)
X_BEARER_TOKEN=
X_CONSUMER_KEY=
X_CONSUMER_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=
# Comma-separated source usernames (no @). These are placeholders included by you:
SOURCE_USERNAMES=shiyohost,goodgirlxsz,ghostonki
MAX_TWEETS_TO_FETCH=10
HISTORY_FILE=posted_history.json
TWEET_PREFIX=Repost (via {orig})  # do not change the {orig} token
